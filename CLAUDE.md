# AIエージェント作業場（Knowledge × Harness）

このリポジトリは、ユーザー専属の「AIエージェント作業場」として機能する。
以降のすべてのタスクにおいて、Claudeは以下の枠組みを常に適用する。

## 定義

- **ナレッジ (Knowledge)**: ユーザーの思考・会社の文脈・過去の業務データ・意思決定の前提。
  `/knowledge` ディレクトリに蓄積し、タスク実行時の参照元とする。
- **ハーネス (Harness)**: Claudeが脱線しないためのルール・制約・出力フォーマット・ガードレール。
  このファイル（CLAUDE.md）自体と `/harness` ディレクトリの各種ルールファイルが該当する。
- **AGI的自律実行**: 曖昧な指示に対しても、ナレッジとハーネスを補完材料として使い、
  9割以上完成された成果物を一発で出力することを目標とする。

## 実行手順（毎タスク共通）

1. 依頼内容を分解し、必要なナレッジ・ハーネスが揃っているか内部で整理する。
2. 完全に自動補完できる場合は、そのまま最高精度の成果物を作成・実行する。
3. 不足データがある場合は、`AskUserQuestion` 等で最小限の質問のみ行う（作業を止めすぎない）。
4. タスク完了後、恒久的に使えるルール・知見が生まれた場合は、このCLAUDE.mdまたは
   `/harness`・`/knowledge` 配下のファイルを更新し、作業場として蓄積する。

## ディレクトリ構成（今後生成）

- `/knowledge` — 業務文脈、過去の意思決定、プロジェクトごとのメモ
- `/harness` — プロジェクト別・タスク種別ごとの制約・出力フォーマット定義
- `/projects` — 個々のプロジェクトの成果物・進行中タスク

## 運用ルール

- 出力は常に「完成品」を目指す。中途半端な下書きではなく、そのまま使える成果物を出す。
- 過剰な確認は避け、合理的な前提を置いて進める。ただし本当に判断がつかない点のみ質問する。
- 新しいプロジェクトを開始する際は、`/projects/<プロジェクト名>/` にまとめ、
  必要なナレッジ・ハーネスをそこに追加していく。
